export default function Footer({className}) {
    return (
        <footer className={`w-full p-4 mt-12 ${className}`}>
            <h3 className="text-center text-sm text-gray-400">
                Compartilhe seu progresso nas redes sociais:
            </h3>
        </footer>
    );
};
