export default function Header({className}) {
    return (
        <header className={`w-full p-10 mb-10 ${className}`}>
            <h1 className="text-center text-4xl">
                Vencendo o vicio!
            </h1>
        </header>
    );
};
